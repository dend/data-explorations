# Spotify Podcast Data Authenticator

A tool that is designed to get the right cookies to authenticate with the Spotify podcasting portal.

To run:

```python
python -m spda
```

Currently the tool is designed around the use of the [Edge web driver](https://developer.microsoft.com/microsoft-edge/tools/webdriver/), but can be easily switched to any other browser.